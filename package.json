{
  "name": "fv-sec-001-solution",
  "version": "1.0.0",
  "private": true,
  "description": "Streaming TypeScript CLI for the FV-SEC001 ad performance aggregation challenge",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/cli.js",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
